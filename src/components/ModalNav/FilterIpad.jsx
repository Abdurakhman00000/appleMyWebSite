import React from 'react';
import ListProduct from '../products/ListProduct';

const FilterIpad = () => {
    return (
        <div>
            <ListProduct/>
        </div>
    );
};

export default FilterIpad;